# Local service configuration

This public snapshot intentionally omits the iOS Firebase configuration file, removes generated iOS build metadata, and replaces embedded Google API keys with `REPLACE_WITH_GOOGLE_API_KEY`.

Before building, download a configuration file for your own Firebase project and place it at `ios/Runner/GoogleService-Info.plist`. Keep it untracked.
